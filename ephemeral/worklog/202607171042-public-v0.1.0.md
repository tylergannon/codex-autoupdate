# Public v0.1.0 worklog

correction: Track `.codex/environments/environment.toml`; the generated Codex environment setup is repository configuration, not disposable local noise.
decision: The README starts with a pasteable Codex prompt and keeps manual installation on the versioned `go install github.com/tylergannon/codex-autoupdate/cmd/codex-autoupdate@v0.1.0` path.
decision: Publish the repository publicly, squash-merge the publication PR, then tag that exact merged `main` commit as `v0.1.0` before proving a fresh public install.
