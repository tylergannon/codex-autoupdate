decision: User requires at least five fully independent reviewers across different model variants; do not partition the codebase, seed hypotheses, or share findings between reviewers.
decision: Findings are admissible only when directly reproducible or established by a short, complete causal argument from observed code and system behavior; speculative races and edge-case guesses are excluded.
decision: This task is a read-only safety audit. Preserve each reviewer writeup independently and do not modify production code.
decision: The installed updater has no observed child-process accumulation, but direct source and live evidence establish unbounded diagnostic-log growth, an abandoned 801 MiB staged bundle, and monotonic rollout-cache retention; review outcomes must distinguish those mechanisms from the reported UI symptom.
finding: Two reviewers independently reproduced that live `~/.claude/remote/ccd-cli/*` processes, including a process granted `mcp__computer-use`, are omitted from the Claude activity report because `isClaudeCodeProcess` does not recognize that executable path and the stdio fallback sees only anonymous pipes.
finding: The host currently has two generations of the same resumed Claude remote session under separate long-lived remote servers, but this audit did not establish that `codex-autoupdate` created them or that they cause the reboot-only Computer Use outage.
decision: Reject the reported ChatGPT external-helper quiescence claim as an instability finding: although command-prefix matching omits the helper, the latest real update proves the old helper exited, replayd stopped and deallocated its session, and a single replacement helper started; survival across replacement was not reproduced.
decision: No reviewer proved that this program caused the machine-wide, reboot-only loss of Computer Use. Report the confirmed safety and accumulation bugs without upgrading them into a root-cause claim.
